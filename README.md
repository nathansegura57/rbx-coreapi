# rbx-coreapi Kernel Precise Specs

This pack is for rewriting the kernel only.

Start with `CLAUDE.md`.

Then read files in `specs/` in numeric order.

The specs intentionally exclude gameplay systems, UI libraries, migration compatibility, lowercase aliases, and tests.
